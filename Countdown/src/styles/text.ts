import {StyleSheet} from 'react-native';
import colors from './colors';

export default StyleSheet.create({
  normalText: {
    color: colors.white,
    fontSize: 15,
  },
  container: {
    display: 'flex',
  },
});
